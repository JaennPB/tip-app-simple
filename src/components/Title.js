import styled from 'styled-components';

const StyledTitle = styled.h1`
  font-size: 3rem;
  font-weight: 300;
`;

const Title = (props) => {
  return <StyledTitle>Tip App</StyledTitle>;
};

export default Title;
